<!-- start responsive navbar header in bootstrap-->
<nav class="navbar transparent navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand">
        <%= link_to image_tag("mentormapp_grey_logo.png", size: "300x50", alt: "MentorMapp Logo" , class: "nav_log"), root_path, id: "mentormapp_logo" %>
      </a>
    </div>
    <div class="container">
      <div class="collapse navbar-collapse" id="myNavbar">
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <!-- Start Dropdown event links -->
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">Find an
                Event
                <span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <% @events.each do |event| %>
                    <li><%= link_to event.location, event_path(event) %></li>
                <% end %>
              </ul>
            </li>
            <!-- End of dropdown dynamic links -->
            <li><%= link_to "Find a Coach", 'http://bit.ly/1P68e2A' %></li>
            <li><%= link_to("Become a Coach", "http://bit.ly/1m5BFI9") %></li>
            <li><%= link_to "Free Trial", new_user_registration_path, { id: 'contact_btn' }%></li>
            <li><%= link_to("Log In",new_user_session_path, id: 'login_btn') if !user_signed_in? %></li>
            <li><%= link_to("Sign Out", destroy_user_session_path, :method => :delete) if user_signed_in? %></li>
          </ul>
        </nav>
      </div>
    </div>
  </div>
</nav>
<!--end of responsive navbar header in bootstrap-->
